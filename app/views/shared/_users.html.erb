<% if @side_bar_users.present? %>
  <ul class="list-unstyled">
  <% @side_bar_users.each do |user| %>
    <li>
      <% if user == @user %>
        <%= user.name %>
      <% else %>
        <%= link_to user.name, list_path(id: user.id) %>
      <% end %>
    </li>
  <% end %>
  </ul>
<% end %>
